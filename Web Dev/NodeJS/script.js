const math = require("./math");
console.log(
  "The addition of 2 and 3 is",
  math.add(2, 3),
  "\n",
  "and the substraction is",
  math.sub(2, 3),
  "\n",
  "The multiplication is ",
  math.mult(2, 3)
);
